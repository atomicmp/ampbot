{
  "name": "ampbot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "discord.js": "^11.4.2",
    "dotenv": "^6.0.0",
    "jest": "^23.5.0",
    "nodemon": "^1.18.3"
  },
  "devDependencies": {
    "eslint": "^5.4.0",
    "prettier-eslint": "^8.8.2"
  },
  "scripts": {
    "start": "nodemon -r dotenv/config src",
    "format": "prettier --write '{src,public}/**/*.js' '{src,public}/*/!(node_modules)/**/*.js'",
    "lint": "eslint {src,public} --ignore-path .gitignore ",
    "test": "echo \"Error: no test specified\" && exit 1",
    "pre-commit-lint": "lint-staged"
  },
  "author": "",
  "license": "ISC"
}
